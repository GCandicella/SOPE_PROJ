#ifndef SOPE_PROJ_MAIN_H
#define SOPE_PROJ_MAIN_H

/**
 *
 * @param name - string to check if it is a dir or just a file
 * @return - 0 if it is a dir, 1 if it isn't, -1 otherwise
 */
int isDir(const char* name);

#endif //SOPE_PROJ_MAIN_H
